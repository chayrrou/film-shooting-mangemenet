{% extends 'base.html.twig' %}

{% block title %}Lieu{% endblock %}

{% block body %}
<style>
*, *::before, *::after { box-sizing:border-box }

body {
	margin:0;
  display:grid;  
  min-height:100vh;
  font-family: Roboto, sans-serif;
 
}

.card {
  padding:34px 54px;
  margin:80px;
  background:#fafafa;
  max-width:450px;
  border-radius:15px;
  margin-left:25%;
  height:28%;
  box-shadow: -5px 5px 10px rgba(0,0,0,0.7),
              5px 5px 10px rgba(0,0,0,0.7);
 
}
 p {
    max-width:90%;
    margin-bottom:20px;
  }
h2{
    font-family: 'Nunito';
    font-size: 35px;
    margin-left:35px;
    padding-left:10px;
    border: 5px solid blue;
    border-top: 0px;
    border-bottom: 0px;
    border-right: 0px;
    width:100%;
}

.location {
border:5px solid blue;
padding:10px;
  font-size: 15px;
  color: #333;
  margin-bottom: 10px;
}

.div{
   display: flex;
  justify-content: center;
  align-items: center;

}

.button {
  display: inline-block;
  padding: 10px 20px;
  background-color: tomato;
  color: white;
  text-decoration: none;
  border-radius: 20px;
  transition: background-color 0.3s ease;
  margin: 10px;
     font-family: 'Nunito';
    text-align:center;
    font-size:20px;
    box-shadow: -5px 5px 10px rgba(255, 255, 255, 0.7),
            5px 5px 10px rgba(255, 255, 255, 0.7);


}


.button:hover {
  background-color: orangered;
}


</style>
<div style="margin-left:50px; margin-top:50px">
    {#<h1>Lieu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lieu.id }}</td>
            </tr>
            <tr>
                <th>LocalisationDeScene</th>
                <td>{{ lieu.localisationDeScene }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lieu_index') }}">back to list</a>

    <a href="{{ path('app_lieu_edit', {'id': lieu.id}) }}">edit</a>

    {{ include('lieu/_delete_form.html.twig') }}#}
</div>

<div class="card">
        <h2>Lieu N° {{ lieu.id }}</h2>
        <div style="margin-top: 15%; text-align: center;">
            <span class="location">Adress : {{ lieu.localisationDeScene }}</span>
        </div>

         <div style="margin-top:15%" class="div">
            <a href="{{ path('app_lieu_index') }}" class="button">Back to List</a>
            <a href="{{ path('app_lieu_edit', {'id': lieu.id}) }}" class="button">Edit</a>
         </div>
</div>
{% endblock %}
