<form method="post" action="{{ path('app_societe_de_production_delete', {'id': societe_de_production.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ societe_de_production.id) }}">
    <button class="btn">Delete</button>
</form>
