<form method="post" action="{{ path('app_tournage_delete', {'id': tournage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournage.id) }}">
    <button class="btn">Delete</button>
</form>
